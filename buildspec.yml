version: 0.1
   
phases:
  install:
    commands:
      - gem install jekyll jekyll-paginate jekyll-sitemap je
      - bundle update
  build:
    commands:
      - echo "******** Building Jekyll site ********"
      - jekyll build
      - ls _site/
      - ls 
      - aws s3 ls s3://mcfalls.me
      - ls _site/
  post_build:
    commands:
      - echo "******** Uploading to S3 ********"
      - aws s3 sync _site/ s3://mcfalls.me